import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme)=>({
    threadsWrapper:{
        padding:"0",
        overflowY:"scroll",
        height:"100%",
    },
}));

export { useStyles };

