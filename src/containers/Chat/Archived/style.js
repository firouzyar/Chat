import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme)=>({
    ListWrapper:{
        padding:"0",
        overflowY:"scroll",
        height:"100%",
    },
}));

export { useStyles };

